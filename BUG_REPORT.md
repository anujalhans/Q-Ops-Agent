# Q-Ops Agent Bug Report

Generated on 2026-05-09.

This report captures issues discovered while creating and executing the Playwright test plan. Status values are `OPEN`, `IN_PROGRESS`, `FIXED`, or `ACCEPTED`.

| Bug ID | Severity | Area | Title | Evidence | Impact | Recommended Fix | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| BUG-001 | Info | Build/Test Infrastructure | Restricted sandbox blocks Vite/esbuild process spawning | Build failed in the default sandbox with `spawn EPERM`, then passed when rerun with approval outside the sandbox | This is an execution-environment constraint, not a confirmed product bug | Run build and Playwright in CI or local shells that allow child process/browser spawning | ACCEPTED |
| BUG-002 | Info | Dependency/Security | Install-time npm audit warning did not reproduce after final dependency state | `npm install` printed `2 moderate severity vulnerabilities`, but follow-up `npm audit --audit-level=moderate` returned `found 0 vulnerabilities` | No active dependency vulnerability is confirmed in the final lockfile | No code fix required; keep `npm audit` in CI to catch future dependency drift | ACCEPTED |
| BUG-003 | Medium | Auth/Routing | Explore page uses legacy `qops-agent-auth` key instead of Supabase session key for authenticated CTA targeting | `src/pages/ExploreMorePage.tsx` checked `qops-agent-auth`, while current auth stores `qops-agent-supabase-session` | Authenticated users opening `/explore` could see CTAs that route like unauthenticated users | Replaced the legacy key check with `qops-agent-supabase-session`; added Playwright coverage for authenticated Explore CTAs | FIXED |
| BUG-004 | Medium | Accessibility/Testability | Login and forgot-password visible field labels are not programmatically associated with inputs | Playwright `getByLabel('Email')` and `getByLabel('Password')` could not locate the modal fields even though visible label text is rendered; tests had to use placeholders | Screen-reader users and label-driven automation could not reliably identify auth fields | Added `htmlFor`/`id` associations for login email, login password, and forgot-password email; Playwright now uses label queries | FIXED |
| BUG-005 | High | Responsive Dashboard | Mobile dashboard sidebar overlays and intercepts main workflow controls | Full Playwright run: desktop Chromium 8/8 passed, mobile public 4/4 passed, but mobile dashboard workflow tests failed because the fixed `w-80` sidebar intercepted clicks on knowledge, document-generation, and settings controls | Authenticated users on phone-sized screens cannot reliably use core dashboard workflows | Add a responsive navigation pattern: collapse sidebar behind a menu/drawer below desktop breakpoints, remove fixed `ml-80` layout on mobile, and ensure main content width/position is viewport-safe | OPEN |
| BUG-006 | Critical | Database Security | Row Level Security is disabled on operational public job tables | Supabase schema inspection on 2026-05-09 reported critical advisory `rls_disabled` for `public.qa_jobs`, `public.doc_ingestion_jobs`, `public.doc_ingestion_queuecreator_logs`, and `public.qa_job_metrics` | Anyone with a Supabase anon/authenticated client key could read or modify all rows in these operational tables, including job inputs, outputs, metrics, and errors | Enable RLS on the four tables and add least-privilege policies for admin workspace access, assigned-project access, service-role workflow writes, and user-owned job visibility before exposing clients broadly | OPEN |
| BUG-007 | High | Knowledge Base Ingestion | Live ingestion job remains stuck in `processing` with no terminal status | Created project `E2E-QOPS-20260509-171521`, uploaded six artifacts through the UI, received job `ING-260509-RH02VR`; DB row stayed `processing` with unchanged `updated_at=2026-05-09 11:50:27.783988`, `output=null`, `error=null`, and metrics only showed `JOB_QUEUED` after repeated polling | Users can be left waiting indefinitely at 50% progress; downstream document generation is blocked and operations must manually diagnose or clean stuck jobs | Add worker-level success/failure finalization around the full vectorization subworkflow, update `doc_ingestion_jobs` to `completed` or `failed` on every path, log `PROCESSING_STARTED`/`JOB_FAILED` metrics, and add a stale-processing timeout/recovery path | OPEN |
| BUG-008 | Medium | Infrastructure Telemetry API | `/webhook/infrastructure-load` returns HTTP 200 with an empty body on direct request | `Invoke-WebRequest http://localhost:5678/webhook/infrastructure-load` returned `StatusCode=200` and `ContentLength=0`; direct browser `fetch` also returned `200` with an empty response body | API consumers and diagnostics cannot distinguish success from missing data; dashboards may rely on stale or fallback telemetry instead of live backend values | Ensure the workflow always responds with valid JSON for success and structured JSON errors for unauthorized/unavailable states; return `401/403` for auth failures instead of an empty `200` | OPEN |

## Execution Notes

The Playwright suite uses mocked Supabase and n8n endpoints for repeatable coverage. Latest mocked-suite execution: `npm run build` passed outside the sandbox, `npm run test:e2e -- --project=chromium` passed 8/8, and full `npm run test:e2e` passed 12/16 with the 4 failures isolated to mobile dashboard responsiveness.

Live localhost E2E validation on 2026-05-09 covered admin login, UI project creation, six-file knowledge-base upload, Supabase DB verification, n8n workflow inspection, infrastructure telemetry probing, logout, and registered-user login. Auth flows passed for both admin and registered user. The knowledge-base ingestion flow created job `ING-260509-RH02VR` but did not reach a terminal backend state during the validation window.
